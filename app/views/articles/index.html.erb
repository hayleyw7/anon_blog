<! –– # rendered via `def show` in `controllers/articles_controller.rb` ––>

<! –– # mix of html & erb ––>

<h1>Anonymous Community Blog</h1>

<h2>Articles</h2>

<! –– # `<% %>` = "evaluate enclosed ruby code & output returned value" ––>

Our blog has <%= Article.public_count %> articles and counting!

<ul class='home-list'>
  <% @articles.each do |article| %>

    <% unless article.archived? %>

      <li class='article-name-list'>

        <! –– # render article title as a link to show.html.erb ––>
        <%= link_to article.title, article, class: "article-links" %>
        
      </li>

    <% end %>

  <% end %>
</ul>

<%= button_to "Create A Post", new_article_path, :method => "get" %>